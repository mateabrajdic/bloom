import PreviewOpenGraphImage, {
  alt,
  contentType,
  size,
} from "../../preview/[id]/opengraph-image";

export const runtime = "nodejs";
export { alt, contentType, size };
export default PreviewOpenGraphImage;
